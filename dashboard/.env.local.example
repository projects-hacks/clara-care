# Copy to .env.local and fill in values.

# API base URL — defaults to production backend
NEXT_PUBLIC_API_URL=https://api.claracare.me

# Set to 'true' to use mock data instead of live backend
NEXT_PUBLIC_USE_MOCK=false

# Default patient ID for the dashboard
NEXT_PUBLIC_PATIENT_ID=patient-dorothy-001

# Backend URL for server-side proxy (used in next.config.ts rewrites)
BACKEND_URL=https://api.claracare.me
