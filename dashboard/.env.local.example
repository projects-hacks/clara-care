# Copy to .env.local and fill in values.
# Leave NEXT_PUBLIC_API_URL empty to use Next.js proxy (rewrites /api/* to BACKEND_URL).

# API base URL for client-side fetch. Empty = same origin, use proxy (see next.config.ts).
NEXT_PUBLIC_API_URL=

# Default patient ID for the dashboard.
NEXT_PUBLIC_PATIENT_ID=patient-dorothy-001

# Backend URL for the /backend-api proxy (server-side). Default: 5001 (5000 is often used by macOS AirPlay).
BACKEND_URL=http://localhost:5001
